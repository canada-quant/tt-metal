# KDA PR Refactor Work Ledger

Purpose: concise, timestamped execution record for the KDA refactor of PR #51910.

Legend: 🧭 plan · 🛠️ in progress · ✅ verified · ⚠️ issue · ⛔ blocked · 💡 decision

## Current status

- **2026-08-06 13:20 UTC** 🧭 Initialized: refactor work begins on `momcilo/kda-pr-refactor`, tracking `origin/momcilo/feature/kda-pr`. Development specification, redesign specification, and supporting evidence are available in `specs/`.

## Work entries

_Add succinct timestamped entries here for progress, problems, failures, resolutions, and decisions._
- **2026-08-06 13:31 UTC** 🛠️ Migrated tracked KDA Python, CPU reference, K3 configuration, and test hierarchy into `models/demos/deepseek_v3_d_p`; direct TTNN convolution and gated-RMS coverage moved to the KDA operation-test tree.
- **2026-08-06 13:31 UTC** 💡 Replaced layer-owned mutable carries with frozen `KdaState`, explicit allocation/validation, and `ttKDA.forward(hidden_states, state) -> (output, next_state)`. Cache keys now use only `layer_{layer_idx}.kda.<weight-name>`; cache-root/invalidation remains caller-owned.
- **2026-08-06 13:31 UTC** ⚠️ Native KDA kernels and legacy orchestration still reside in the GDN leaf; this is the next extraction concern, so boundary validation is intentionally not yet expected to pass.
- **2026-08-06 13:46 UTC** ✅ Restored `chunk_gated_delta_rule/` byte-for-byte to `origin/main` and registered a KDA-owned native leaf. The prescribed `./build_metal.sh` passes and installs the worktree Python bindings.
- **2026-08-06 13:46 UTC** ⚠️ The KDA leaf is mechanically isolated but still uses the transitional aggregated `chunk_kda` front-end and phased implementation. It must be split into the six specified KDA operation leaves and Python graph orchestration before this concern is complete.
- **2026-08-06 13:54 UTC** ✅ Repaired KDA kernel relocation: runtime paths now target the KDA leaf, the required final-scan writer is present with the seven-argument KDA accessor ABI, and existing vector-gate KDA readers/compute kernels are selected. `./build_metal.sh` passes; `scripts/run_safe_pytest.sh models/demos/deepseek_v3_d_p/tests/kda/model/test_layer.py -q -s` reports `9 passed` and `SAFE_PYTEST_RESULT: PASS` (first-case PCC: output 0.999961, recurrent 0.999900, convolution 0.999997).
- **2026-08-06 14:00 UTC** ⚠️ The operation-level `T=5120,H=2,K=V=32,summary_group_chunks=8` case reproduces a device-dispatch timeout twice (standard and `--dev` watcher runs), while shorter cases and the composed-layer suite pass. The watcher captured a 44.480s dump. CPU oracle phase logging now flushes cache state and elapsed time before device work; the existing T=5120 real-weight cache path also flushes hit/miss and elapsed time.
- **2026-08-06 14:05 UTC** ✅ Diagnosed and repaired the T=5120 hang: summary scan produces one `[K,V]` pair, while the copied final writer incorrectly awaited `NC` token slabs. The KDA writer now drains that single summary pair. The exact hardware case passes under the unchanged 5s dispatch guard (`CPU=2.237s`; test body `3.25s`; output PCC `0.999962`; state PCC `0.999966`; `SAFE_PYTEST_RESULT: PASS`).
- **2026-08-06 14:08 UTC** ✅ Migrated remaining model, distributed, real-weight, and perf callers to explicit `KdaState` input/output. Updated the moved KDA test instructions. Static compilation and the composed Blackhole suite pass (`9 passed`, `SAFE_PYTEST_RESULT: PASS`); first composed case PCC: output `0.999961`, recurrent `0.999900`, convolution `0.999997`.
- **2026-08-06 14:08 UTC** 💡 Split design confirmed from source: the current KDA leaf remains a transitional monolith (`chunk_kda`, shared `vector_gate` modes, and C++ collective/layout orchestration). The next concern is a six-leaf native extraction—prep, final scan, affine compose, affine prefix, causal convolution, gated RMS—then replace the monolithic calls with Python-owned recurrence, prefix, and halo graphs. No performance tuning will be folded into that extraction.- **2026-08-06 14:15 UTC** ✅ Committed the validated relocation and device-liveness repair as `e37ac0b02e0`; the required GDN boundary remains byte-identical to `origin/main`.
- **2026-08-06 14:18 UTC** ✅ Registered KDA leaf kernels for runtime installation (`1ce3e361247`) and began the gated-RMS leaf: leaf-owned operation types (`3bbdfea954a`), kernel manifest (`f9590cb02a4`), factory interface (`ed974c275af`), and compiled factory translation unit (`bb5dc59b01f`). Each concern had a fresh `origin/main` fetch and passing `./build_metal.sh` or pre-commit validation.

- 2026-08-06 14:32 UTC ✅ Extracted gated-RMS into a self-owned KDA leaf (public API, nanobind binding, device operation, factory, kernels, and source manifest); moved the reused work-distribution/compute-config code into neutral KDA factory utilities. Initial compile exposed empty fallback-created leaf files and two namespace qualifications; populated/corrected them, then `./build_metal.sh` passed. `scripts/run_safe_pytest.sh tests/ttnn/nightly/unit_tests/operations/transformer/kda/test_gated_rms_norm.py -q -s` passed 5/5 on Blackhole with `dispatch_timeout=5s`, finite outputs, trace/cache coverage, determinism, and minimum PCC 0.999998. GDN boundary remains exact to origin/main.

- **2026-08-06 14:38 UTC** ✅ Extracted four-tap causal convolution into its self-owned KDA leaf (public API, explicit nanobind binding, device operation/types, factory, kernels, and source registration). `./build_metal.sh` passed. `scripts/run_safe_pytest.sh tests/ttnn/nightly/unit_tests/operations/transformer/kda/test_convolution.py -q -s` passed 4/4 on Blackhole under `dispatch_timeout=5s`, including channel widths 512/1024/2048 and determinism. GDN boundary remains exact to `origin/main`.

- **2026-08-06 14:59 UTC** ✅ Registered distinct affine-prefix and affine-composition device-operation leaves, with composition's dedicated dataflow ABI omitting initial state and its compute kernel ending after affine summary composition. `./build_metal.sh` passed after resolving unity-build namespace/helper collisions. `scripts/run_safe_pytest.sh models/demos/deepseek_v3_d_p/tests/kda/operations/test_distributed_affine.py -q -s` passed 10/10 on Blackhole under `dispatch_timeout=5s`; all checked tensors were finite, trace/cache and bit-identical determinism passed, minimum observed PCC was `0.999991`, and JIT reported `171/171` cache hits.

- **2026-08-06 17:42 UTC** ✅ Extracted chunk preparation into a self-owned KDA operation leaf with public API/binding, validation/output specs, factory, kernel registration, and focused TTNN coverage. The transitional composed recurrence now calls the dedicated primitive. `./build_metal.sh` passed; direct prep plus composed layer validation passed 11/11 on Blackhole under `dispatch_timeout=5s`, including cache reuse, trace replay, bit-identical determinism, state purity, finiteness, and PCCs of output `0.999961`, recurrent `0.999900`, and convolution `0.999997`.
- **2026-08-06 17:42 UTC** ⚠️ Resolved three extraction-time failures: unavailable `ruff` (`exit 127`, used installed `black --check`), collection failure caused by the pre-existing deletion of required `tt/kda/ops.py` (restored the transitional module pending Python orchestration), and a compile error from the obsolete monolithic `vector_gate=true` argument (vector gating is intrinsic to the dedicated KDA leaf).
- **2026-08-06 17:43 UTC** ⚠️ First local commit attempt was rejected by repository hooks: `gersemi` reformatted the leaf manifest and `prefer-expect-error` required the shared validation fixture. Accepted the mechanical formatting and replaced `pytest.raises` with `expect_error` before retrying validation.
- **2026-08-06 18:10 UTC** ✅ Extracted final chunk scan into a self-owned KDA operation leaf with public API/binding, strict shape/dtype validation, output specs, program factory, and focused TTNN coverage. The transitional composed recurrence now calls the dedicated primitive for ordinary, grouped-summary, and state-only scans. `./build_metal.sh` passes.
- **2026-08-06 18:10 UTC** ⚠️ Resolved two extraction defects from measured failures. Unity compilation first exposed names colliding with the transitional monolith, so the new leaf types were made operation-specific. The first hardware run then timed out on two worker cores; comparison with the source factory showed that the extracted factory omitted the kernel-required `c_22` new-value circular buffer. Restoring the exact buffer mapping removed the hang under the unchanged 5s dispatch guard.
- **2026-08-06 18:10 UTC** ✅ Direct final-scan validation passed 2/2 on Blackhole, including CPU-reference agreement, finiteness, program-cache reuse, trace replay, bit-identical determinism, and rejection of BF16 recurrent state. The full direct-plus-caller suite passed 26/26 (`SAFE_PYTEST_RESULT: PASS`, 217.71s): composed model/state/cache tests and the complete chunk envelope, including grouped summaries at T=2816/5120/5152 and weak-decay invariance. Minimum reported output/state PCC was `0.999918` in the HiFi2 case; T=5120 production output/state PCC was `0.999962`/`0.999966`.
- **2026-08-06 18:33 UTC** ✅ Completed the public APIs and nanobind registration for the dedicated affine-composition and affine-prefix leaves. `./build_metal.sh` passes. Direct Blackhole coverage passes 3/3 under the 5s dispatch guard for FP32/BF16 summaries, finite CPU-oracle agreement, cache reuse, trace replay, bit-identical determinism, and BF16-state rejection; minimum PCC was `0.999997`.
- **2026-08-06 18:33 UTC** ⚠️ Focused affine validation exposed and resolved three extraction-time defects: a unity regrouping collision between generic `check` helpers, a missing composition `c_9` output-page descriptor, and a stale TensorAccessor start offset of 5 after the dedicated composition ABI removed the old `compose_only` scalar (correct offset 4). The first two hardware attempts produced non-finite or scrambled composition output; the corrected leaf reports 3/3 pass and 24/24 JIT cache hits.
- **2026-08-06 18:41 UTC** ✅ Reimplemented convolution halo exchange and distributed affine prefix as typed Python TTNN graphs in `tt/kda/ops.py`; their tests now call those Python APIs instead of the transitional aggregate bindings. The exact C++ ordering was preserved: padded TILE tail gather for halo and packed A/B gather followed by rank-serial matmul/add carry propagation for affine prefix.
- **2026-08-06 18:41 UTC** ✅ `scripts/run_safe_pytest.sh models/demos/deepseek_v3_d_p/tests/kda/operations/test_halo.py models/demos/deepseek_v3_d_p/tests/kda/operations/test_distributed_affine.py -q -s` passed 14/14 on the 2x4 Blackhole mesh (`FABRIC_1D`, 14.69s, 197/197 JIT hits). Both TP/SP axis orientations, FP32/BF16 summary and recurrent-state combinations, exact causal carries, serial-prefix agreement, trace replay, and bit-identical determinism passed.
- **2026-08-06 19:00 UTC** ✅ Replaced the transitional `chunk_kda` call with Python-owned recurrence orchestration over the dedicated preparation, final-scan, affine-composition, and affine-prefix leaves. The graph preserves flat/non-flat inputs, padding, grouped summaries, distributed prefix, explicit initial/final state, output layout, and production dtype/kernel controls. The preparation leaf now owns the aggregate's exact circular-buffer plus per-bank output fit calculation and chooses L1 or DRAM when placement is unspecified; distributed summaries explicitly retain DRAM.
- **2026-08-06 19:00 UTC** ✅ `./build_metal.sh` passed. Direct preparation plus recurrence validation passed 17/17, composed-layer validation passed 9/9, and the final corrected halo-plus-recurrence matrix passed 19/19 on Blackhole under the 5s dispatch guard (`SAFE_PYTEST_RESULT: PASS`, 340/340 JIT hits). Coverage includes T=32 through T=5152, flat/non-flat and padded forms, grouped-prefix T=5120, weak-decay preservation, causal halo on both mesh axes, and bit-identical determinism. T=5120 production output/state PCC remained `0.999962`/`0.999966`; the composed T=32 output/recurrent/convolution PCCs remained `0.999961`/`0.999900`/`0.999997`.
- **2026-08-06 19:00 UTC** ⚠️ Pre-commit diff review caught a mechanical memory-config edit on the halo graph instead of the preparation call. Corrected the call sites before commit, reran syntax checks and the full affected 19-test hardware matrix, and observed no failures.
- **2026-08-06 19:09 UTC** ✅ Removed the transitional KDA aggregate C++ API/bindings and phased factories (`chunk_kda`, C++ distributed affine prefix, and C++ convolution halo), leaving six independently registered KDA leaves plus the neutral factory utility. `./build_metal.sh` passed, and the worktree Python surface confirms all three aggregate bindings are absent.
- **2026-08-06 19:09 UTC** ✅ The complete post-deletion affected suite passed 54/54 on the 2x4 Blackhole mesh under the 5s dispatch guard (`SAFE_PYTEST_RESULT: PASS`, 119.72s, 538/538 JIT hits): all six direct TTNN leaves, Python recurrence/prefix/halo orchestration, and the composed model/state/cache suite. All numerical checks were finite; minimum observed PCC remained `0.999918` in the HiFi2 recurrence state case.
- **2026-08-06 19:22 UTC** ⚠️ Final-envelope validation exposed three caller/test defects rather than layer defects: distributed constructors omitted their selected SP axis, segmented-state reconstruction read the original rather than returned recurrent carry, and two 2D weight tests used the obsolete `tp_axis` keyword plus an outdated output-projection program-config signature. Corrected the callers to mirror the production layer boundary and grid selection.
- **2026-08-06 19:22 UTC** 💡 Renamed the test checkpoint helper from `checkpoint.py` to `checkpoint_utils.py`; the former collided with the retained `checkpoint/` package and prevented combined-suite collection. Removed the last tracked experimental-package initializer while preserving unrelated untracked review material in that directory.
- **2026-08-06 19:22 UTC** ✅ Distributed layer validation passed 8/8 on the 2x4 Blackhole mesh (`SAFE_PYTEST_RESULT: PASS`, 43.74s), covering both TP/SP axis orientations, serial-reference agreement, T=5120 segmented continuity, state propagation, and determinism. Representative TP-axis-1 PCCs were output `0.999944`, recurrent `0.999917`, and convolution `0.999997`; segmented output and carries were bit-identical (`PCC=1.0`).
- **2026-08-06 19:22 UTC** ✅ Weight placement/determinism validation passed 8 tests with 6 expected alternate-mesh skips (`SAFE_PYTEST_RESULT: PASS`, 19.51s), including TP8 and both supported 2D tensor-parallel axes. A clean checkpoint, reference, cache-fingerprint, state-purity, and CPU-determinism run passed 46/46 (`KDA_RUN_LONG_TESTS=1`, `SAFE_PYTEST_RESULT: PASS`, 16.46s); split output and all logical carries reported `PCC=1.0` with unchanged input state.
- **2026-08-06 19:31 UTC** ⚠️ The first composed trace test incorrectly required a newly allocated returned convolution carry to act as a trace-stable mutable cache. Three measured materialization attempts (slice destination, clone, and preallocated copy) did not change that replay-only carry address behavior, while composed output and recurrent results remained bit-identical. Removed the attempted production changes: trace-stable mutable state is an explicit non-goal, and the supported immutable contract is capture/replay of the forward output without modifying caller-supplied state.
- **2026-08-06 19:31 UTC** ✅ Added a hermetic full-layer trace gate using the production 256 MiB trace-region contract and fallback exceptions. It warms, captures, replays, verifies bit-identical output, verifies replacement state tensors are distinct, and proves both supplied carries remain bit-identical. The focused gate passed 1/1; the complete composed-layer suite passed 10/10 on Blackhole (`SAFE_PYTEST_RESULT: PASS`, 9.46s, 90/90 JIT hits), with finite output/state PCCs unchanged (`0.999961`/`0.999900`/`0.999997`).
- **2026-08-06 20:49 UTC** ✅ Closed real-checkpoint coverage against the Kimi-K3 snapshot `9f62e4e9fffbd0a83ddd60e1c209d828994b3569`. Parent-owned tensor-cache roots are now placement-specific (`sp1_tp8`, `sp2_tp4`, `sp4_tp2`) and weight APIs use the canonical `tensor_parallel_axis` keyword. The real-weight suite passed 3/3 across the production layouts; all tensors were finite and minimum output/recurrent/convolution PCCs were `0.999900`/`0.999800`/`0.999999`.
- **2026-08-06 20:49 UTC** ⚠️ The first immutable-graph performance run passed SP2/SP4 but measured SP1 at `10.386-10.402 ms` versus the `10.126 ms` limit. Matched raw device traces against commit `3ac2325517b` showed every program through output projection unchanged; only the final Linear reduce-scatter grew from about `1.01 ms` to `1.57 ms`. Rejected experiments included state placement/copies, grouped-state compaction, folded scan arithmetic, explicit deallocation/order changes, cluster-axis selection, and reduce-scatter worker/chunk/buffer tuning. A controlled Ring trial restored SP1 to `9.786 ms`; Ring on a 2D TP row is unroutable (`M0,D0` to `M0,D3`), so the default is now mesh-aware: Ring for flat SP1 and Linear when sequence parallelism uses the 2D mesh. Explicit topology overrides remain honored.
- **2026-08-06 20:49 UTC** ✅ Final Blackhole LoudBox acceptance passed 3/3 with `FABRIC_1D`, production `T=5120`, 10 trace replays, independent FP32 CPU oracle, and finite outputs/carries: SP1xTP8 `9.786 ms <= 10.126 ms`, SP2xTP4 `9.734 ms <= 10.058 ms`, and SP4xTP2 `10.195 ms <= 10.789 ms`. Corresponding output PCCs were `0.999902`, `0.999902`, and `0.999904`; recurrent PCCs were `0.999800`, `0.999925`, and `0.999926`; convolution PCC was `0.999999` throughout. Command: `KIMI_K3_CKPT=<checkpoint> scripts/run_safe_pytest.sh models/demos/deepseek_v3_d_p/tests/kda/perf/test_layer_perf.py -s -q` (`SAFE_PYTEST_RESULT: PASS`, 14.24s, 462/462 JIT hits).
- **2026-08-06 20:49 UTC** ✅ Completed the typed device-residency audit for all three production cells. `specs/kda-device-residency-audit.json` validates as `DeviceResidencyAudit(status="pass")` with 3 workload cells and 20 source/runtime-backed device entries. Torch use is confined to construction-time weights/constants; the complete forward variants contain no Torch conversion, host transfer, host fallback, or unknown placement. Static removal checks also found no tracked experimental KDA files and no `chunk_kda`, layer-owned mutable-state, or legacy class symbols; the native KDA tree contains exactly the six required leaves plus neutral factory utilities.
- **2026-08-06 20:58 UTC** ✅ Terminal validation passed on the final installed build. `./build_metal.sh` completed successfully. `KIMI_K3_CKPT=<checkpoint> KDA_RUN_LONG_TESTS=1 scripts/run_safe_pytest.sh --run-all models/demos/deepseek_v3_d_p/tests/kda --ignore=models/demos/deepseek_v3_d_p/tests/kda/perf -q -s` passed 104 tests with 6 expected complementary-mesh skips (`SAFE_PYTEST_RESULT: PASS`, 80.91s, 1421/1443 JIT hits); the production T=5120 operation case remained finite at output/state PCC `0.999962`/`0.999966`. The post-build production performance matrix passed 3/3 again (`SAFE_PYTEST_RESULT: PASS`, 14.60s, 462/462 JIT hits); SP4xTP2 measured `10.185 ms <= 10.789 ms`, with all endpoint PCCs unchanged.
- **2026-08-06 21:02 UTC** ✅ Final performance-distribution reporting passed on the Blackhole eight-device mesh with `FABRIC_1D`, real Kimi-K3 weights, production `T=5120`, five synchronized samples of ten trace replays each, and the existing first-sample acceptance metric. Acceptance/median/worst-tail timing was SP1xTP8 `9.784/9.798/9.820 ms` (limit `10.126 ms`), SP2xTP4 `9.732/9.740/9.749 ms` (limit `10.058 ms`), and SP4xTP2 `10.190/10.196/10.222 ms` (limit `10.789 ms`). Output PCC was `0.999902`/`0.999902`/`0.999904`, recurrent PCC was `0.999800`/`0.999925`/`0.999926`, convolution PCC was `0.999999`, and all endpoints were finite. Command: `KIMI_K3_CKPT=<checkpoint> scripts/run_safe_pytest.sh models/demos/deepseek_v3_d_p/tests/kda/perf/test_layer_perf.py -s -q` (`3 passed`, `SAFE_PYTEST_RESULT: PASS`, 15.68s, 462/462 JIT hits).
